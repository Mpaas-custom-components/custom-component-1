<view class="container">
  <dropdown items={{dayOptions}} defaultValue={{calendar.day}} />
  <dropdown items={{monthOptions}} defaultValue={{calendar.month}} />
  <dropdown items={{yearOptions}} defaultValue={{calendar.year}} />
</view>
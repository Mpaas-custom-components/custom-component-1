<button
  onTap="onTap"
  class="{{classData}} {{size ? size : 'large'}}"
  type="{{type}}"
  loading={{loading}}
  disabled={{disabled}}
  form-type="{{formType}}"
  style="background : {{bgColor}}; color : {{fontColor}}"
  name="{{name}}"

  >
  <slot></slot>
</button>